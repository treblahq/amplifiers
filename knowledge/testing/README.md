# Testing

> Defines the minimal testing strategy for skills and stacks.

Primary test groups:

- `tests/skill-triggering/`
- `tests/skill-composition/`
- `tests/explicit-requests/`

Focus first on:

- correct trigger behavior
- correct composition suggestions
- stable discovery of the canonical skill paths
