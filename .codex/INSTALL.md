# Codex instructions

The canonical runtime files live in `skills/`.

Use `.agents/README.md` as the lightweight pointer that explains where the
runtime skills actually live.

Do not create a second authored copy of the skills inside `.agents/`.
